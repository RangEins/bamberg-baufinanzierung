---
interface Props {
  delay?: number;
  as?: string;
  class?: string;
}

const { delay = 0, as: Tag = 'div', class: className = '' } = Astro.props;
---

<Tag data-reveal data-reveal-delay={delay} class={className}>
  <slot />
</Tag>
